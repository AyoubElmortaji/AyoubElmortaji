# ---------------------------------------------------------------------------
# Copy this file to `.env.local` and fill in the values.
# `.env.local` is git-ignored — never commit it.
# ---------------------------------------------------------------------------

# Canonical site URL, used for metadata / Open Graph / sitemap absolute URLs.
# Set this to your real domain once it is live.
NEXT_PUBLIC_SITE_URL=https://ayoub-elmortaji.vercel.app
